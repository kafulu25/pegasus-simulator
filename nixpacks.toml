[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx vite build"]

[phases.build.cache]
directories = ["node_modules/.cache"]

[start]
cmd = "npx serve -s dist -l 3000"

[build.env]
NODE_ENV = "production"
CI = "false"
